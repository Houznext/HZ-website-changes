const fs = require('fs');
const path = require('path');

const out = path.join(__dirname, '../src/components/crm/LeadFormModal.tsx');
const D = 'd' + 'iv';
const o = (cls, extra = '') => `<${D} className="${cls}"${extra}>`;
const c = `</${D}>`;

const lines = [];
const w = (s) => lines.push(s);

w(`'use client';`);
w('');
w(`import { useEffect, useState } from 'react';`);
w(`import toast from 'react-hot-toast';`);
w(`import adminApi from '@/lib/axios';`);
w(`import { BHK_OPTS, BUDGET_RANGES, PROPERTY_TYPES, SOURCES } from './crmConstants';`);
w('');
w(`const AGENTS = ['Arjun Sharma', 'Priya Reddy', 'Kiran Kumar'];`);
w('');
w(`type Props = {`);
w(`  open: boolean;`);
w(`  onClose: () => void;`);
w(`  defaultStage?: string;`);
w(`  onCreated?: () => void;`);
w(`};`);
w('');
w(`const UsersIcon = () => (`);
w(`  <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="#fff" strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round">`);
w(`    <path d="M17 21v-2a4 4 0 00-4-4H5a4 4 0 00-4 4v2" />`);
w(`    <circle cx="9" cy="7" r="4" />`);
w(`    <path d="M23 21v-2a4 4 0 00-3-3.87M16 3.13a4 4 0 010 7.75" />`);
w(`  </svg>`);
w(`);`);
w('');
w(`const CloseIcon = () => (`);
w(`  <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round">`);
w(`    <line x1="18" y1="6" x2="6" y2="18" />`);
w(`    <line x1="6" y1="6" x2="18" y2="18" />`);
w(`  </svg>`);
w(`);`);
w('');
w(`const PlusIcon = () => (`);
w(`  <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.5" strokeLinecap="round">`);
w(`    <line x1="12" y1="5" x2="12" y2="19" />`);
w(`    <line x1="5" y1="12" x2="19" y2="12" />`);
w(`  </svg>`);
w(`);`);
w('');
w(`export function LeadFormModal({ open, onClose, defaultStage, onCreated }: Props) {`);
w(`  const [submitting, setSubmitting] = useState(false);`);
w(`  const [linkProperty, setLinkProperty] = useState(false);`);
w(`  const [form, setForm] = useState({`);
w(`    fullName: '', phone: '', email: '',`);
w(`    source: 'Website enquiry', propertyType: 'Apartment',`);
w(`    bhkPreference: '', budgetRange: '', preferredCity: '', preferredLocality: '',`);
w(`    purpose: 'Self use', loanRequired: 'Yes', timeline: '3–6 months',`);
w(`    priority: 'cold' as 'hot' | 'warm' | 'cold',`);
w(`    assignedTo: '', notes: '', stage: defaultStage || 'new',`);
w(`  });`);
w('');
w(`  useEffect(() => {`);
w(`    if (open) setForm((f) => ({ ...f, stage: defaultStage || f.stage || 'new' }));`);
w(`  }, [open, defaultStage]);`);
w('');
w(`  const resetForm = () => {`);
w(`    setForm({`);
w(`      fullName: '', phone: '', email: '', source: 'Website enquiry', propertyType: 'Apartment',`);
w(`      bhkPreference: '', budgetRange: '', preferredCity: '', preferredLocality: '',`);
w(`      purpose: 'Self use', loanRequired: 'Yes', timeline: '3–6 months',`);
w(`      priority: 'cold', assignedTo: '', notes: '', stage: defaultStage || 'new',`);
w(`    });`);
w(`    setLinkProperty(false);`);
w(`  };`);
w('');
w(`  const handleClose = () => { resetForm(); onClose(); };`);
w('');
w(`  const priorityPills = [`);
w(`    { val: 'hot' as const, emoji: '🔥', label: 'Hot', a: { bg: '#fee2e2', color: '#dc2626', border: '#fca5a5' }, i: { bg: '#fff', color: '#5a6a7e', border: '#e2e8f0' } },`);
w(`    { val: 'warm' as const, emoji: '🟡', label: 'Warm', a: { bg: '#fff7ed', color: '#c2410c', border: '#fed7aa' }, i: { bg: '#fff', color: '#5a6a7e', border: '#e2e8f0' } },`);
w(`    { val: 'cold' as const, emoji: '🔵', label: 'Cold', a: { bg: '#eff6ff', color: '#2563eb', border: '#93c5fd' }, i: { bg: '#fff', color: '#5a6a7e', border: '#e2e8f0' } },`);
w(`  ];`);
w('');
w(`  const handleSubmit = async () => {`);
w(`    if (!form.fullName.trim()) { toast.error('Full name is required'); return; }`);
w(`    if (!form.phone.trim()) { toast.error('Phone is required'); return; }`);
w(`    setSubmitting(true);`);
w(`    try {`);
w(`      await adminApi.post('/admin/crm/leads', {`);
w(`        ...form,`);
w(`        email: form.email || undefined,`);
w(`        assignedTo: form.assignedTo || undefined,`);
w(`        bhkPreference: form.bhkPreference || undefined,`);
w(`        budgetRange: form.budgetRange || undefined,`);
w(`        preferredCity: form.preferredCity || undefined,`);
w(`        preferredLocality: form.preferredLocality || undefined,`);
w(`        notes: form.notes || undefined,`);
w(`        stage: form.stage,`);
w(`        priority: form.priority,`);
w(`      });`);
w(`      toast.success('Lead created ✓ · Email sent to business@houznext.com');`);
w(`      resetForm();`);
w(`      onClose();`);
w(`      onCreated?.();`);
w(`    } catch (err: unknown) {`);
w(`      const msg = err && typeof err === 'object' && 'response' in err`);
w(`        ? (err as { response?: { data?: { message?: string } } }).response?.data?.message`);
w(`        : undefined;`);
w(`      toast.error(msg || 'Failed to create lead');`);
w(`    } finally { setSubmitting(false); }`);
w(`  };`);
w('');
w(`  if (!open) return null;`);
w('');
w(`  return (`);
w(`    ${o('overlay', ' onClick={(e) => { if (e.target === e.currentTarget) handleClose(); }}')}`);
w(`      ${o('modal', ' onClick={(e) => e.stopPropagation()}')}`);
w(`        ${o('modal-hd')}`);
w(`          <${D} style={{ display: 'flex', alignItems: 'center', gap: 12 }}>`);
w(`            <${D} style={{ width: 40, height: 40, borderRadius: 11, flexShrink: 0, background: 'linear-gradient(135deg, #2f80ed, #1a6dd6)', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>`);
w(`              <UsersIcon />`);
w(`            ${c}`);
w(`            <${D}>`);
w(`              <${D} style={{ fontFamily: "'Montserrat',sans-serif", fontSize: 16, fontWeight: 700, color: '#1f2933' }}>Add new lead${c}`);
w(`              <${D} style={{ fontSize: 12, color: '#5a6a7e', marginTop: 2 }}>Real estate buyer / investor enquiry${c}`);
w(`            ${c}`);
w(`          ${c}`);
w(`          <button type="button" onClick={handleClose}`);
w(`            style={{ border: 'none', background: 'transparent', cursor: 'pointer', padding: 6, borderRadius: 8, color: '#5a6a7e', display: 'flex' }}`);
w(`            onMouseEnter={(e) => { e.currentTarget.style.background = '#f1f5f9'; }}`);
w(`            onMouseLeave={(e) => { e.currentTarget.style.background = 'transparent'; }}`);
w(`          >`);
w(`            <CloseIcon />`);
w(`          </button>`);
w(`        ${c}`);
w(`        ${o('modal-bd')}`);
w(`          <${D} style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 12 }}>`);
w(`            <${D}><label className="lbl">Full name <span style={{ color: '#dc2626' }}>*</span></label>`);
w(`            <input className="fi" type="text" placeholder="Enter full name" value={form.fullName} onChange={(e) => setForm({ ...form, fullName: e.target.value })} />${c}`);
w(`            <${D}><label className="lbl">Phone <span style={{ color: '#dc2626' }}>*</span></label>`);
w(`            <input className="fi" type="tel" placeholder="+91 XXXXX XXXXX" value={form.phone} onChange={(e) => setForm({ ...form, phone: e.target.value })} />${c}`);
w(`            <${D}><label className="lbl">Email</label>`);
w(`            <input className="fi" type="email" placeholder="email@example.com" value={form.email} onChange={(e) => setForm({ ...form, email: e.target.value })} />${c}`);
w(`            <${D}><label className="lbl">Lead source</label>`);
w(`            <select className="fi" value={form.source} onChange={(e) => setForm({ ...form, source: e.target.value })}>`);
w(`              {SOURCES.map((s) => <option key={s} value={s}>{s}</option>)}`);
w(`            </select>${c}`);
w(`            <${D}><label className="lbl">Interested in (type)</label>`);
w(`            <select className="fi" value={form.propertyType} onChange={(e) => setForm({ ...form, propertyType: e.target.value })}>`);
w(`              {PROPERTY_TYPES.map((s) => <option key={s} value={s}>{s}</option>)}`);
w(`            </select>${c}`);
w(`            <${D}><label className="lbl">BHK preference</label>`);
w(`            <select className="fi" value={form.bhkPreference} onChange={(e) => setForm({ ...form, bhkPreference: e.target.value })}>`);
w(`              <option value="">—</option>`);
w(`              {BHK_OPTS.map((s) => <option key={s} value={s}>{s}</option>)}`);
w(`            </select>${c}`);
w(`            <${D}><label className="lbl">Budget range</label>`);
w(`            <select className="fi" value={form.budgetRange} onChange={(e) => setForm({ ...form, budgetRange: e.target.value })}>`);
w(`              <option value="">—</option>`);
w(`              {BUDGET_RANGES.map((s) => <option key={s} value={s}>{s}</option>)}`);
w(`            </select>${c}`);
w(`            <${D}><label className="lbl">Preferred city</label>`);
w(`            <select className="fi" value={form.preferredCity} onChange={(e) => setForm({ ...form, preferredCity: e.target.value })}>`);
w(`              <option value="">Select city</option><option>Hyderabad</option><option>Bengaluru</option><option>Chennai</option><option>Mumbai</option>`);
w(`            </select>${c}`);
w(`            <${D}><label className="lbl">Preferred locality</label>`);
w(`            <input className="fi" type="text" placeholder="e.g. Gachibowli, Kondapur" value={form.preferredLocality} onChange={(e) => setForm({ ...form, preferredLocality: e.target.value })} />${c}`);
w(`            <${D}><label className="lbl">Purpose</label>`);
w(`            <select className="fi" value={form.purpose} onChange={(e) => setForm({ ...form, purpose: e.target.value })}>`);
w(`              <option>Self use</option><option>Investment</option><option>Rental income</option>`);
w(`            </select>${c}`);
w(`            <${D}><label className="lbl">Loan required</label>`);
w(`            <select className="fi" value={form.loanRequired} onChange={(e) => setForm({ ...form, loanRequired: e.target.value })}>`);
w(`              <option>Yes</option><option>No</option><option>Pre-approved</option>`);
w(`            </select>${c}`);
w(`            <${D}><label className="lbl">Timeline</label>`);
w(`            <select className="fi" value={form.timeline} onChange={(e) => setForm({ ...form, timeline: e.target.value })}>`);
w(`              <option>Immediate (0–1 month)</option><option>1–3 months</option><option>3–6 months</option><option>6–12 months</option><option>Just exploring</option>`);
w(`            </select>${c}`);
w(`            <${D}><label className="lbl">Priority</label>`);
w(`            <${D} style={{ display: 'flex', gap: 7 }}>`);
w(`              {priorityPills.map((p) => {`);
w(`                const active = form.priority === p.val;`);
w(`                const s = active ? p.a : p.i;`);
w(`                return (`);
w(`                  <button key={p.val} type="button" onClick={() => setForm({ ...form, priority: p.val })}`);
w(`                    style={{ flex: 1, padding: '7px 0', borderRadius: 8, border: \`1.5px solid \${s.border}\`, background: s.bg, color: s.color, fontFamily: "'Montserrat',sans-serif", fontSize: 12, fontWeight: 700, cursor: 'pointer', transition: 'all 150ms', display: 'flex', alignItems: 'center', justifyContent: 'center', gap: 4 }}>`);
w(`                    {p.emoji} {p.label}`);
w(`                  </button>`);
w(`                );`);
w(`              })}`);
w(`            ${c}${c}`);
w(`            <${D}><label className="lbl">Assign to</label>`);
w(`            <select className="fi" value={form.assignedTo} onChange={(e) => setForm({ ...form, assignedTo: e.target.value })}>`);
w(`              <option value="">Unassigned</option>`);
w(`              {AGENTS.map((name) => <option key={name} value={name}>{name}</option>)}`);
w(`            </select>${c}`);
w(`          ${c}`);
w(`          <${D} style={{ marginTop: 12 }}>`);
w(`            <label className="lbl">Notes / requirements</label>`);
w(`            <textarea className="fi" rows={2} style={{ resize: 'none' }} placeholder="Any specific notes about this lead…" value={form.notes} onChange={(e) => setForm({ ...form, notes: e.target.value })} />`);
w(`          ${c}`);
w(`          <${D} style={{ marginTop: 10, display: 'flex', alignItems: 'center', gap: 8, padding: '10px 12px', background: '#fffbeb', border: '1px solid #fef3c7', borderRadius: 9, cursor: 'pointer' }} onClick={() => setLinkProperty(!linkProperty)}>`);
w(`            <input type="checkbox" readOnly checked={linkProperty} style={{ accentColor: '#d97706', width: 14, height: 14, cursor: 'pointer' }} />`);
w(`            <span style={{ fontSize: 13, fontWeight: 600, color: '#92400e', userSelect: 'none' }}>Specific property interested in (link property from listings)</span>`);
w(`          ${c}`);
w(`        ${c}`);
w(`        ${o('modal-ft')}`);
w(`          <button className="btn btn-blue" type="button" onClick={() => void handleSubmit()} disabled={submitting}>`);
w(`            {submitting ? 'Creating…' : (<><PlusIcon /> Create lead</>)}`);
w(`          </button>`);
w(`          <button className="btn btn-ghost" type="button" onClick={handleClose}>Cancel</button>`);
w(`        ${c}`);
w(`      ${c}`);
w(`    ${c}`);
w(`  );`);
w(`}`);
w('');

fs.writeFileSync(out, lines.join('\n'), 'utf8');
console.log('Wrote', out);
